import { FunctionComponent } from "react";
import { Nav } from "react-bootstrap";

export const Sidebar: FunctionComponent = () => {
  return <Nav className="flex-column border-end h-100 bg-white p-0"></Nav>;
};
